{
  "name": "ade",
  "private": true,
  "scripts": {
    "test": "npm run test --prefix apps/desktop && npm run test --prefix apps/mcp-server",
    "test:ci": "npm run test:coverage --prefix apps/desktop && npm run test --prefix apps/mcp-server"
  }
}
