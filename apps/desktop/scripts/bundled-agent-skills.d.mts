// Types for bundled-agent-skills.mjs so the TypeScript drift test in
// src/shared/bundledAgentSkills.test.ts can import the packaging roster
// directly instead of scraping it out of the validator's source text.
export const BUNDLED_AGENT_SKILLS: readonly string[];
