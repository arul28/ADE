/**
 * Moved to `@ade-dev/ui`.
 *
 * Plugin pages need the same class merger the app uses, so it lives in the
 * published kit and the app imports it from there. This file stays as the
 * import path 200-odd renderer modules already use.
 */
export { cn } from "@ade-dev/ui";
