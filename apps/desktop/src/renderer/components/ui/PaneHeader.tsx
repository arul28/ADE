/** Moved to `@ade-dev/ui`. */
export { PaneHeader } from "@ade-dev/ui";

/** See `BUTTON_TAILWIND_ANCHOR` in `./Button` for why this string exists. */
export const PANE_HEADER_TAILWIND_ANCHOR =
  "flex shrink-0 items-center justify-between px-4 min-h-[36px] min-w-0 flex items-center gap-2 truncate font-mono text-[10px] font-bold tracking-[1px] uppercase text-[#71717A] select-none text-[9px] text-[#52525B] gap-1.5";
