import React from "react";
import { EmptyState } from "../ui/EmptyState";

export function ConflictsPage() {
  return <EmptyState title="Conflicts" description="Phase 4 adds conflict radar + proposals + apply UI." />;
}

