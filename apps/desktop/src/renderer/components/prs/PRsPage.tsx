import React from "react";
import { EmptyState } from "../ui/EmptyState";

export function PRsPage() {
  return <EmptyState title="PRs" description="Phase 1+ adds PR linkage, checks/reviews, and stack landing." />;
}

