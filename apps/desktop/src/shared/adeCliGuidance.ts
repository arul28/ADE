export const ADE_CLI_AGENT_GUIDANCE = [
  "## ADE CLI",
  "`ade` is the default control plane for ADE-managed sessions: lanes, missions, PRs, chats/sessions, memory, proof, config, and process state.",
  "If `command -v ade` fails, try `${ADE_CLI_PATH:-}` when set, then `${ADE_CLI_BIN_DIR:-}/ade`, and in an ADE source checkout fall back to `node apps/ade-cli/dist/cli.cjs ...` after confirming the file exists.",
  "The only normal reason to skip ADE CLI for an ADE action is that the user truly does not have it installed or reachable after those fallbacks.",
  "For ADE work beyond the immediate local edit, shell command, or repository inspection in front of you, check ADE CLI first: run `ade doctor` if needed, use typed commands like `ade lanes list --text` / `ade prs checks <pr> --text`, or discover with `ade actions list --text` and `ade actions run ...`.",
  "iOS Simulator and Preview Lab control is only supported from ADE desktop chats. If you are in a standalone CLI session outside an ADE chat, do not try to drive `ade ios-sim`; tell the user to open or rerun the request in an ADE chat.",
  "Use `ade help <command>` and `ade help <command> <subcommand>` to get precise flags instead of guessing. For iOS Simulator and Preview Lab work, start with `ade help ios-sim`, then drill into `ade help ios-sim launch`, `snapshot`, `previews`, `preview-render`, `select`, `tap`, `drag`, `type`, or `shutdown` as needed. Use `ade --socket ios-sim actions --text` for the raw ios_simulator action catalog.",
  "For App Control work on Electron apps, prefer desktop socket mode so the CLI and ADE drawer share the same managed app session. Start with `ade help app-control` and `ade --socket app-control status --text`; launch with `ade --socket app-control launch --command \"npm run dev\" --text` or connect to an existing debuggable app with `ade --socket app-control connect --cdp-port <port> --text`. To reuse a Run-tab process, list configured commands via `ade --socket settings get --text`, then launch with `--command` plus `--cwd` matching that process so the app runs from the same directory the Run tab does (relative cwds resolve against the lane root, and the launch is rejected if the resolved cwd escapes the lane). Launches run in the visible chat terminal; ADE sets ADE_APP_CONTROL_CDP_PORT and ADE_APP_CONTROL_DEBUG_FLAGS in the environment and auto-forwards debug flags for common npm/pnpm/yarn/bun script launches and direct electron commands. Custom launchers should forward one of those values to Electron's --remote-debugging-port.",
  "After an App Control launch or when the user asks what happened in that visible launch terminal, first run `ade --socket app-control status --text` or `--json`; do not say you lack terminal visibility until App Control status and terminal list have both been checked. Prefer `ade --socket app-control logs --text --max-bytes 8388608` to inspect full scrollback, `ade --socket app-control terminal write --data \"y\\n\"` to answer prompts, and `ade --socket app-control terminal signal --signal SIGINT` to interrupt the active App Control launch. If there is no active App Control terminal, fall back to `ade --socket terminal list --text --limit 20` and choose the running/recent `App Control:` terminal, or use `ADE_CHAT_SESSION_ID` with `ade --socket terminal read --chat-session \"$ADE_CHAT_SESSION_ID\" --text`. After the Electron renderer connects, use `ade --socket app-control snapshot --text` for screenshot + DOM refs, `click` / `type` for Control-mode input, and the drawer's Inspect mode (or `ade --socket app-control select --x <x> --y <y>`) to attach screenshot-backed UI context to chat. App Control is a bridge over existing tools; Playwright, agent-browser, browser-use, or Computer Use can still be useful, but ADE should preserve the launch state, terminal output, screenshot, DOM/selector packet, source candidates, and proof/context attachments.",
  "For iOS Simulator work, prefer desktop socket mode so the CLI and ADE drawer share one long-lived simulator service: run `ade --socket ios-sim status --text`, `ade --socket ios-sim devices --text`, `ade --socket ios-sim apps --device <udid> --text`, then `ade --socket ios-sim launch --target <id> --text` (or `--bundle-id`/`--app-bundle`). Launch keeps Simulator.app in the background by default so the ADE drawer remains the control plane.",
  "After an iOS app is launched, use `ade --socket ios-sim snapshot --text` or `ade --socket ios-sim elements --text` for screenshot + accessibility/ADEInspector grounding, then `ade --socket ios-sim select --x <x> --y <y>` to add selected UI context to the drawer chat. Use `tap`, `drag`/`swipe`, and `type` against the active launched app; use `window-start`, `live-start`, `preview-start`, `stream-status`, and `stream-stop` only for visual stream management. If the simulator is not running or no active session/snapshot is available, warn the user with the exact blocker instead of guessing the screen. When you finish, run `ade --socket ios-sim shutdown` to tear down the session, stop streaming, and release the idb companion.",
  "For ADE Preview Lab work, use `ade --socket ios-sim preview-status --text` to check Xcode MCP readiness, `ade --socket ios-sim previews --source <swift-file> --text` to discover existing `#Preview`/`PreviewProvider` targets, and `ade --socket ios-sim preview-render --source <swift-file> --index <n> --text` as the final open/render step. If no matching preview exists, add one first; if one exists, reuse it.",
  "When changing SwiftUI from simulator or preview context, keep the affected UI previewable. Add or repair nearby `#Preview` definitions and deterministic preview fixtures when needed, preferably in feature sidecar files such as `<Feature>Previews.swift` or a DEBUG-only `PreviewSupport` helper. Preview fixtures should use representative mock data derived from visible UI context when possible, but must not depend on live sync, keychain, network, push, sockets, or a production database.",
  "When preview appearance matters, add named light/dark preview variants with `.preferredColorScheme(.light)` or `.preferredColorScheme(.dark)` and prefer adaptive system colors over hardcoded light-only or dark-only values. Do this as part of making the SwiftUI surface previewable, not as an app-specific special case.",
  "When asked to make a SwiftUI preview reachable in the live simulator, add a DEBUG-only route, deep link, launch-argument handler, or small preview host that presents the same view with the same deterministic fixtures. ADE may pass `ADE_PREVIEW_*` environment values when launching from a selected preview target; use them when helpful, but keep the implementation app-local and optional.",
  "When an iOS visual inspect packet is attached to chat, treat previewability as part of the work: ensure the affected source file or a nearby related Swift file contains a renderable `#Preview`/`PreviewProvider`, and add mock data or a preview harness if the screen would otherwise be blank.",
  "When the user asks you to capture, send, attach, or provide proof, use whatever computer-use or browser tool is appropriate to produce the evidence, then register it with ADE via `ade proof ...` so it appears in the ADE proof drawer for the active chat, mission, or lane.",
  "When you run processes of any kind, track what you started and clean up old, stale, or finished processes before leaving the task.",
].join("\n");

export const ADE_CLI_INLINE_GUIDANCE =
  "`ade` is the default control plane for ADE-managed sessions: lanes, missions, PRs, chats/sessions, memory, proof, config, and process state. If `command -v ade` fails, try `${ADE_CLI_PATH:-}`, then `${ADE_CLI_BIN_DIR:-}/ade`, and in an ADE source checkout `node apps/ade-cli/dist/cli.cjs ...` after confirming it exists. The only normal reason to skip ADE CLI for an ADE action is that the user truly does not have it installed or reachable after those fallbacks. For ADE work beyond the immediate local edit, shell command, or repository inspection in front of you, check ADE CLI first: try `ade doctor`, typed `ade ... --text` commands, `ade help <command>`, `ade help <command> <subcommand>`, or `ade actions list --text` / `ade actions run ...`. iOS Simulator and Preview Lab control is only supported from ADE desktop chats; if you are in a standalone CLI session outside an ADE chat, do not try to drive `ade ios-sim`, and tell the user to open or rerun the request in an ADE chat. For App Control work on Electron apps, prefer desktop socket mode so the CLI and drawer share the same managed app session: run `ade help app-control`, `ade --socket app-control status --text`, launch with `ade --socket app-control launch --command \"npm run dev\" --text`, or connect with `connect --cdp-port <port> --text`. To reuse a Run-tab process, discover its command and cwd via `ade --socket settings get --text`, then launch with `--command` and a matching `--cwd` (relative cwds resolve against the lane root and a cwd outside the lane is rejected). App Control launches run in the visible chat terminal; for terminal/log questions first use `ade --socket app-control logs --text --max-bytes 8388608`, `ade --socket app-control terminal write --data \"y\\n\"`, or `ade --socket app-control terminal signal --signal SIGINT`, and only fall back to `ade --socket terminal read --chat-session \"$ADE_CHAT_SESSION_ID\" --text` or `terminal list` when no active App Control terminal exists. ADE sets ADE_APP_CONTROL_CDP_PORT and ADE_APP_CONTROL_DEBUG_FLAGS and auto-forwards debug flags for common npm/pnpm/yarn/bun script launches and direct electron commands; custom launchers should forward one of those values to Electron's --remote-debugging-port. After launch, use `snapshot --text` / `elements --text`, Control mode or `click`/`type` to drive the app, and Inspect mode or `select --x <x> --y <y>` to attach screenshot-backed DOM/selector/source context to chat. For iOS Simulator work inside an ADE chat, use desktop socket mode when drawer state matters and discover precise flags with `ade help ios-sim` plus focused pages such as `ade help ios-sim launch`, `snapshot`, `previews`, `preview-render`, `select`, `tap`, `drag`, `type`, and `shutdown`; raw actions are listed by `ade --socket ios-sim actions --text`. Start with `ade --socket ios-sim status --text`, `devices --text`, `apps --device <udid> --text`, launch with `ade --socket ios-sim launch --target <id> --text`, then use `snapshot --text` / `elements --text`, `select --x <x> --y <y>`, `tap`, `drag`/`swipe`, and `type` against the active launched app; stream commands are `window-start`, `live-start`, `preview-start`, `stream-status`, and `stream-stop`; tear down with `ade --socket ios-sim shutdown`. If the simulator is not running or no active session/snapshot is available, warn the user with the exact blocker instead of guessing the screen. For ADE Preview Lab, check `preview-status --text`, discover with `previews --source <swift-file> --text`, and finish with `preview-render --source <swift-file> --index <n> --text`; add a preview only when no matching preview already exists. When changing SwiftUI from simulator or preview context, keep the affected UI previewable: add or repair nearby `#Preview` definitions and deterministic mock fixtures in feature sidecar files or DEBUG-only preview support, using visible UI context as representative data when useful and avoiding live sync/keychain/network/database dependencies. When preview appearance matters, add named light/dark preview variants with `.preferredColorScheme(.light)` or `.preferredColorScheme(.dark)` and prefer adaptive system colors over hardcoded light-only or dark-only values. When asked to make a SwiftUI preview reachable in the live simulator, add a DEBUG-only route, deep link, launch-argument handler, or small preview host that presents the same view with the same deterministic fixtures. ADE may pass `ADE_PREVIEW_*` environment values when launching from a selected preview target; use them when helpful, but keep the implementation app-local and optional. When an iOS visual inspect packet is attached, ensure the affected source file or a nearby related Swift file contains a renderable `#Preview`/`PreviewProvider`, and add mock data or a preview harness if the screen would otherwise be blank. When the user asks you to capture, send, attach, or provide proof, use whatever computer-use or browser tool is appropriate to produce the evidence, then register it with ADE via `ade proof ...` so it appears in the ADE proof drawer for the active chat, mission, or lane. When you run processes of any kind, track what you started and clean up old, stale, or finished processes before leaving the task.";
