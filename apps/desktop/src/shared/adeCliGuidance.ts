export const ADE_CLI_AGENT_GUIDANCE = [
  "## ADE CLI",
  "ADE is a local-first desktop development environment. It manages lanes (git worktrees), native ADE chats, terminal sessions, missions, PR workflows, memory, proof/artifacts, App Control, iOS Simulator/Preview Lab state, lane-tied macOS VMs, config, and managed processes.",
  "`ade` is the default control plane for ADE-managed sessions. Use normal shell commands for the immediate repo inspection/edit/test in front of you; use ADE CLI when you need ADE state, drawer/session state, proof registration, missions, PR metadata, memory, or managed app/simulator control.",
  "",
  "### First orientation",
  "- For ADE work beyond the immediate local edit, shell command, or repository inspection in front of you, check ADE CLI first.",
  "- Start with `ade doctor --text` when the environment is unclear.",
  "- Use typed commands with `--text` for readable output: `ade lanes list --text`, `ade missions list --text`, `ade chats list --text`, `ade prs checks <pr> --text`, `ade proof status --text`, and `ade actions list --text`.",
  "- Use `ade help <command>` and `ade help <command> <subcommand>` to discover exact flags instead of guessing. `ade actions list --text` / `ade actions run ...` is the escape hatch for service actions that do not yet have a friendly command.",
  "- If `command -v ade` fails, try `${ADE_CLI_PATH:-}` when set, then `${ADE_CLI_BIN_DIR:-}/ade`, and in an ADE source checkout fall back to `node apps/ade-cli/dist/cli.cjs ...` after confirming the file exists.",
  "- The only normal reason to skip ADE CLI for an ADE action is that the user truly does not have it installed or reachable after those fallbacks.",
  "",
  "### Desktop socket surfaces",
  "- Use `--socket` when the ADE desktop drawer and the CLI must share one live session. This matters for App Control, iOS Simulator, Preview Lab, terminal logs, selection/context capture, and proof drawer updates.",
  "- iOS Simulator and Preview Lab control is only supported from ADE desktop chats. If you are in a standalone CLI session outside an ADE chat, do not try to drive `ade ios-sim`; tell the user to open or rerun the request in an ADE chat.",
  "- For App Control on Electron apps, prefer socket mode: `ade help app-control`, `ade --socket app-control status --text`, `ade --socket app-control launch --command \"npm run dev\" --text`, or `ade --socket app-control connect --cdp-port <port> --text`.",
  "- For App Control terminal/log questions, check `ade --socket app-control status --text`, then prefer `ade --socket app-control logs --text --max-bytes 8388608`, `ade --socket app-control terminal write --data \"y\\n\"`, or `ade --socket app-control terminal signal --signal SIGINT`. Only fall back to `ade --socket terminal list --text` / `terminal read` when no App Control terminal is active.",
  "- ADE sets `ADE_APP_CONTROL_CDP_PORT` and `ADE_APP_CONTROL_DEBUG_FLAGS` for App Control launches. Custom Electron launchers should forward one of those values to `--remote-debugging-port`.",
  "- After App Control launch/connect, use `ade --socket app-control snapshot --text` or `elements --text`; use Control mode or `click`/`type` for input; use Inspect mode or `select --x <x> --y <y>` to attach screenshot-backed DOM/selector/source context to chat.",
  "- For ADE browser work, prefer socket mode so CLI calls and the Work sidebar share the same global browser tabs: `ade help browser`, `ade --socket browser panel --text`, `ade --socket browser status --text`, `ade --socket browser open <url> --new-tab --text`, `ade --socket browser switch --tab <id> --text`, `ade --socket browser screenshot --text`, and `ade --socket browser inspect-start --text` / `ade --socket browser select-current --text` / `ade --socket browser clear-selection --text`. The ADE browser is global rather than lane-scoped; links from chat output and localhost URLs in chat terminals should open there by default.",
  "- For lane-tied macOS VM work, use `ade help macos-vm`, `ade --socket macos-vm status --lane <lane> --text`, `ade --socket macos-vm start --lane <lane> --create --text`, and `ade --socket macos-vm guide --lane <lane> --text`. To interact through ADE, use `ade --socket macos-vm screenshot --lane <lane> --text`, `ade --socket macos-vm select --lane <lane> --x <x> --y <y> --text`, `ade --socket macos-vm click --lane <lane> <x> <y>`, and `ade --socket macos-vm type --lane <lane> --value <text>`; click/select coordinates are window-relative by default.",
  "",
  "### iOS Simulator and Preview Lab",
  "- For iOS work inside an ADE chat, start with `ade help ios-sim`, `ade --socket ios-sim status --text`, `ade --socket ios-sim devices --text`, and `ade --socket ios-sim apps --device <udid> --text`, then launch with `ade --socket ios-sim launch --target <id> --text`.",
  "- For iOS launch target problems, do not create symlink projects, fake schemes, or repo-layout shims as the first fix. Re-list `ade --socket ios-sim apps --text`; ADE should discover root-level `.xcodeproj` bundles and `apps/*/*.xcodeproj` projects. If no app appears or the build fails, report the selected project, scheme, and exact build output instead of guessing.",
  "- After launch, use `ade --socket ios-sim snapshot --text` or `ade --socket ios-sim elements --text` for screenshot + accessibility/ADEInspector grounding. Use `ade --socket ios-sim select --x <x> --y <y>` to attach context to the drawer chat, and `ade --socket ios-sim tap`, `ade --socket ios-sim drag` / `ade --socket ios-sim swipe`, or `ade --socket ios-sim type` against the active app.",
  "- Stream commands are `ade --socket ios-sim window-start`, `ade --socket ios-sim live-start`, `ade --socket ios-sim preview-start`, `ade --socket ios-sim stream-status`, and `ade --socket ios-sim stream-stop`. Use `ade --socket ios-sim stream-status --text` to explain the active backend/input path, fallback reason, helper pid, latency, and any blocker. Low idle fps is normal on `iosurface-indigo` because frames are event-driven while the simulator is still.",
  "- For ADE Preview Lab, check `ade --socket ios-sim preview-status --text`, discover existing previews with `ade --socket ios-sim previews --source <swift-file> --text`, and finish with `ade --socket ios-sim preview-render --source <swift-file> --index <n> --text`. Add a preview only when no matching preview already exists.",
  "- If the simulator is not running or no active session/snapshot is available, warn the user with the exact blocker instead of guessing the screen.",
  "- When you finish iOS Simulator work, run `ade --socket ios-sim shutdown` when you own the session and the task no longer needs the running simulator.",
  "",
  "### SwiftUI previewability",
  "- When changing SwiftUI from simulator or preview context, keep the affected UI previewable: add or repair nearby `#Preview` definitions and deterministic mock fixtures when needed.",
  "- Preview fixtures should use representative visible UI context when useful, but must not depend on live sync, keychain, network, push, sockets, or a production database.",
  "- For appearance-sensitive work, add named light/dark preview variants with `.preferredColorScheme(.light)` / `.preferredColorScheme(.dark)` and prefer adaptive system colors.",
  "- When asked to make a preview reachable in the live simulator, add a DEBUG-only route, deep link, launch-argument handler, or small preview host. ADE may pass optional `ADE_PREVIEW_*` environment values.",
  "- When an iOS visual inspect packet is attached to chat, ensure the affected source file or a nearby related Swift file contains a renderable `#Preview`/`PreviewProvider`, and add mock data or a preview harness if the screen would otherwise be blank.",
  "",
  "### Proof and cleanup",
  "- When the user asks you to capture, send, attach, or provide proof, use the appropriate computer-use/browser/app-control tool to produce evidence, then register it with ADE via `ade proof ...` so it appears in the ADE proof drawer for the active chat, mission, or lane.",
  "- When you run processes of any kind, track what you started and clean up old, stale, or finished processes before leaving the task.",
].join("\n");

export const ADE_CLI_INLINE_GUIDANCE = [
  "ADE quick orientation:",
  "- ADE is a local-first desktop development environment for lanes (git worktrees), native chats, terminals, missions, PRs, memory, proof/artifacts, App Control, iOS Simulator/Preview Lab state, and lane-tied macOS VMs.",
  "- `ade` is the default control plane for ADE-managed sessions. Use shell for the immediate repo edit/test; use ADE CLI for ADE state, drawer/session state, proof registration, memory, missions, PR metadata, and managed app/simulator/VM control.",
  "- First checks: `ade doctor --text`, `ade help <command>`, typed `ade ... --text` commands, and `ade actions list --text`. Common starts: `ade lanes list --text`, `ade chats list --text`, `ade proof status --text`.",
  "- If `command -v ade` fails, try `${ADE_CLI_PATH:-}`, then `${ADE_CLI_BIN_DIR:-}/ade`, then `node apps/ade-cli/dist/cli.cjs ...` in an ADE source checkout after confirming it exists. The only normal reason to skip ADE CLI for an ADE action is that it is truly unreachable.",
  "- Use `--socket` when the CLI and ADE desktop drawer need the same live state: App Control, iOS Simulator, Preview Lab, terminal logs, selections/context, and proof drawer updates.",
  "- iOS Simulator and Preview Lab control is only supported from ADE desktop chats. For iOS work use `ade help ios-sim`, `ade --socket ios-sim status --text`, `ade --socket ios-sim devices --text`, `ade --socket ios-sim apps --text`, `ade --socket ios-sim snapshot --text`, `ade --socket ios-sim select --x <x> --y <y>`, `ade --socket ios-sim stream-status --text`, and Preview Lab commands such as `ade --socket ios-sim preview-status --text`, `ade --socket ios-sim previews --source <swift-file> --text`, `ade --socket ios-sim preview-render --source <swift-file> --index <n> --text`. Do not create symlink projects or fake schemes as a first fix for app detection; re-list ADE's launch targets and report exact build output.",
  "- For App Control on Electron apps use `ade help app-control`, `ade --socket app-control status --text`, `ade --socket app-control launch --command ... --text`, `ade --socket app-control logs --text --max-bytes 8388608`, `ade --socket app-control snapshot --text`, `ade --socket app-control select --x <x> --y <y>`, `ade --socket app-control click`, and `ade --socket app-control type`.",
  "- For ADE browser use `ade help browser`, `ade --socket browser panel --text`, `ade --socket browser status --text`, `ade --socket browser open <url> --new-tab --text`, `ade --socket browser switch --tab <id> --text`, `ade --socket browser screenshot --text`, and `ade --socket browser inspect-start --text` / `ade --socket browser select-current --text`. The ADE browser is global, not lane-scoped.",
  "- For macOS VM work use `ade help macos-vm`, `ade --socket macos-vm status --lane <lane> --text`, `ade --socket macos-vm start --lane <lane> --create --text`, `ade --socket macos-vm guide --lane <lane> --text`, `ade --socket macos-vm screenshot --lane <lane> --text`, `ade --socket macos-vm select --lane <lane> --x <x> --y <y> --text`, `ade --socket macos-vm click --lane <lane> <x> <y>`, and `ade --socket macos-vm type --lane <lane> --value <text>`; keep code edits under the guest shared path.",
  "- If a live app/simulator/session is missing, report the exact blocker instead of guessing. When asked for proof, register artifacts with `ade proof ...` so they appear in the ADE proof drawer, and clean up old, stale, or finished processes before leaving the task.",
].join("\n");
