# W10: .ade/ Portable State
> Source: [OpenClaw — "file as truth" principle](https://velvetshark.com/articles/openclaw-memory-masterclass) — config files on disk are the source of truth, DB syncs from them. [Paperclip — log integrity](https://github.com/paperclipai/paperclip/blob/main/doc/SPEC.md) — SHA-256 chain for append-only logs. [Symphony — dynamic config reload](https://github.com/openai/symphony/blob/main/SPEC.md) — file-watch for runtime config changes.

Dependencies: **W1** (CTO state files), **W6½** (memory engine).

Consolidates the project-level `.ade/` directory structure, git-tracking policy, and configuration split (shared vs. local vs. secret). This workstream builds on storage that W1 (CTO state) and W6 (memory) already populate today. It also adds startup validation, integrity checking, and migration tooling.

##### Audit Snapshot (2026-03-11)

- Already in code today: file-backed CTO state under `.ade/cto/`, file-backed worker state under `.ade/agents/`, and `.ade/ade.yaml` + `.ade/local.yaml` config loading.
- Current limitation: ADE still excludes the entire `.ade/` directory via `.git/info/exclude`, so the selective tracked/shareable model described here is not live behavior yet.
- Not yet implemented: startup validation/integrity services, log hash chaining, config hot reload, and the actual git-portable subset policy.

##### Complete Directory Structure

```
.ade/
├── cto/
│   ├── identity.yaml          # CTO persona, model preferences, policy config
│   ├── core-memory.json       # CTO Tier 1: persona and project context
│   ├── memory/                # CTO Tier 2: hot memories + archive/
│   └── sessions.jsonl         # CTO session history log
├── agents/
│   ├── backend-dev/
│   │   ├── identity.yaml      # Worker persona, adapter config, heartbeat policy
│   │   ├── core-memory.json   # Worker Tier 1 core memory
│   │   ├── memory/            # Worker Tier 2 + archive
│   │   └── sessions.jsonl     # Worker session log
│   └── ...
├── templates/
│   ├── bug-fix.yaml           # Mission template: bug fixes
│   ├── feature.yaml           # Mission template: features
│   └── refactor.yaml          # Mission template: refactors
├── context/
│   ├── PRD.ade.md             # AI-generated project context
│   └── ARCHITECTURE.ade.md   # AI-generated architecture context
├── memory/
│   ├── project.json           # Project-level facts (Tier 2/3)
│   ├── learning-pack.json     # Auto-curated knowledge (W7)
│   └── archive/               # Cold storage (Tier 3)
├── history/
│   └── missions.jsonl         # Mission run log (append-only)
├── reflections/               # Orchestrator reflections (Phase 3 P7)
│   ├── <mission-id>.jsonl
│   └── retrospectives/
├── ade.yaml                   # Shareable project defaults (target W10 split)
├── local.yaml                 # Machine-local override config
├── local.secret.yaml          # Gitignored: machine-specific secrets
├── ade.db                     # cr-sqlite synced (Phase 6): ALL app state (gitignored)
├── ade.db-wal                 # WAL file (gitignored)
├── mcp.sock                   # Runtime socket (gitignored)
├── embeddings.db              # sqlite-vec embeddings cache (gitignored, regenerated)
├── artifacts/                 # Proof-of-work artifacts (gitignored)
├── transcripts/               # Machine-specific logs (gitignored)
├── cache/                     # Machine-specific cache (gitignored)
├── worktrees/                 # Machine-specific lane checkouts (gitignored)
└── secrets/                   # Machine-specific secrets (gitignored)
```

##### Target Git-Tracking Policy

- **Tracked** (committable): `cto/`, `agents/`, `templates/`, `context/`, `memory/`, `history/`, `reflections/`, `ade.yaml` — tiny config/state files for version tracking and team sharing.
- **Gitignored** (machine-specific): `local.secret.yaml`, `ade.db*`, `mcp.sock`, `embeddings.db`, `artifacts/`, `transcripts/`, `cache/`, `worktrees/`, `secrets/`.

Today this policy is aspirational. The current implementation excludes `.ade/` wholesale, so W10 is the workstream that would make this selective split real.

W7c skill exports currently remain under `.claude/skills/`, but the intended follow-through is to consolidate durable exported artifacts under `.ade/` as part of W10/W7c cleanup.

##### Target Configuration Split

- `ade.yaml` (shareable): Lane templates, phase profiles, feature flags, automation definitions, and project-level defaults.
- `local.yaml` (machine-local): personal overrides, local AI/provider preferences, CLI paths, and other developer-local tweaks.
- `local.secret.yaml` (gitignored, limited use): API keys, external MCP server configs, imported/legacy tokens, local paths, and OAuth secrets where a feature needs them.

##### App State Sync Strategy

Git tracks code, config, and portable agent state. cr-sqlite (Phase 6) syncs app state (`ade.db`). Embeddings are local-only and regenerated on new machines. Memory JSON files are the "file as truth" source — DB syncs from files on startup if they diverge.

##### Startup Validation

On app startup, `adeProjectService.ts` validates the `.ade/` directory:

1. **Structure check**: Verify all expected directories exist. Create missing ones with correct permissions.
2. **Schema validation**: Validate `local.yaml` against a JSON Schema. Warn on unknown keys, error on invalid types. Validate all `identity.yaml` files against the `AgentIdentity` schema.
3. **Secret detection**: Scan tracked files for potential secrets (API keys, tokens). Warn if secrets found in tracked files — suggest moving to `local.secret.yaml`.
4. **Integrity check**: For append-only files (`missions.jsonl`, `sessions.jsonl`), verify each line is valid JSON. Truncate corrupted trailing entries with a warning.
5. **File-as-truth sync**: Compare on-disk identity files against DB `agents` table. If disk is newer (by mtime), update DB from disk. If DB is newer (no file), write file from DB. Log sync actions.
6. **Missing secret warning**: If features require `local.secret.yaml` entries (e.g., Linear sync enabled but no Linear token), show a non-blocking banner: "Linear sync requires API token — [Configure in Settings]".

##### Log Integrity

Adapted from [Paperclip's SHA-256 log chain](https://github.com/paperclipai/paperclip/blob/main/doc/SPEC.md): append-only JSONL files include a hash chain for tamper detection.

- **Hash chain**: Each JSONL entry includes a `prevHash` field containing `sha256(previous_entry_json)`. The first entry uses `prevHash: "genesis"`.
- **Verification**: On startup, `logIntegrityService.ts` samples 10% of entries (or all if < 100 entries) and verifies the hash chain. Broken chain → warning in UI + diagnostic log, but does not block startup.
- **Scope**: Applied to `missions.jsonl` and `sessions.jsonl`. Not applied to `reflections/` (orchestrator-managed, already has its own integrity via `ORCHESTRATOR_OVERHAUL.md` Phase 7).

##### Dynamic Config Reload

Watch `local.yaml` for changes (using `fs.watch`) and hot-reload affected services without app restart:

- Lane templates → reload `laneTemplateService`
- Phase profiles → reload `phaseEngine`
- Automation rules → reload `automationService`
- Agent model preferences → reload on next activation (no mid-run changes)
- Feature flags → reload affected services

Debounce: 2-second window. Only reload services whose config sections actually changed (diff-based).

##### Migration

Move any existing state from pre-Phase-4 storage into the `.ade/` structure:

- **Context packs** → project memory entries (W6 handles this)
- **Agent state from DB-only** → `.ade/agents/<name>/identity.yaml` + `core-memory.json`
- **CTO state from DB-only** → `.ade/cto/identity.yaml` + `core-memory.json`
- **One-time migration**: Runs on first startup after Phase 4 ships. Sets a `migration_v4_completed` flag in DB. Idempotent — safe to re-run.

##### Service Architecture

```
adeProjectService.ts     — Directory structure management, startup validation, secret detection
logIntegrityService.ts   — Hash chain verification for append-only JSONL files
configReloadService.ts   — File-watch hot-reload for local.yaml, diff-based service reload
migrationService.ts      — One-time state migration from pre-Phase-4 storage
```

##### Renderer

- **Settings > Project > .ade/ Structure**: Visual tree showing the `.ade/` directory with file sizes, last modified dates, and tracked/gitignored status.
- **Settings > Project > Configuration**: Split editor for `ade.yaml` (shareable target) and `local.yaml` (machine-local override) with schema validation, syntax highlighting, and real-time error indicators. Separate panel for `local.secret.yaml` with masked values where supported.
- **Settings > Project > Health**: Integrity check results, migration status, file-truth sync log.
- **First-run banner**: If `.ade/` doesn't exist, show: "Initialize ADE project structure? [Initialize] [Skip]". Initialize creates the full directory tree and generates `.gitignore`.

**Implementation status:** Partial foundation exists; portable/shareable policy and supporting services are still pending.

**Tests:**
- Directory structure: startup creates missing directories, correct permissions set, idempotent on re-run.
- Schema validation: valid `local.yaml` passes, missing required field warns, invalid type errors, unknown keys warn.
- Secret detection: API key pattern in tracked file triggers warning, secrets in gitignored file do not warn.
- File-as-truth sync: disk identity newer than DB → DB updated, DB identity with no file → file created, both match → no action.
- Log integrity: valid hash chain passes verification, broken chain detected and warned, genesis entry handled correctly.
- Dynamic config reload: `local.yaml` change triggers service reload within 3s, only affected services reload, rapid changes debounced.
- Migration: pre-Phase-4 agent state migrated to `.ade/agents/`, migration flag prevents re-run, idempotent on manual re-trigger.
- Config files round-trip via git: tracked files preserve through clone/checkout.
- `local.secret.yaml` isolation: never appears in git status after `.gitignore` setup.
- Startup validation: missing directories created, schema validation catches malformed `local.yaml`.
- First-run: `.ade/` initialization creates full tree, `.gitignore` generated with correct patterns.
