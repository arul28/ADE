Findings

High: chat computer-use policy is advisory, not enforced. The session prompt can explicitly say computer use is off or that local fallback is disallowed in agentChatService.ts (line 771), but chat sessions still merge workflow tools unconditionally in agentChatService.ts (line 2849), and captureScreenshot still executes local screencapture in workflowTools.ts (line 138). That means policy is currently “please don’t” instead of a hard boundary.

High: the local computer-use proof loop still is not end-to-end. screenshot_environment and record_environment only create files and return artifact metadata in mcpServer.ts (line 3043) and mcpServer.ts (line 3067); the actual ADE ownership/linkage path is still a separate ingest_computer_use_artifacts call in mcpServer.ts (line 3092). So proof can still exist on disk without counting for mission/PR/Linear closeout. Your own roadmap still flags this in README.md (line 55).

Medium: reportCompletion looks half-shipped. Agents are told to use it in systemPrompt.ts (line 96), and the tool returns a completion_report object in workflowTools.ts (line 209), but there is no downstream consumer beyond that definition/prompt pair. So “completion reporting” is not yet a real ADE state transition or closeout hook.

Medium: external MCP is good enough for baseline use, but not actually “finished-finished.” W8 itself still documents three open follow-through items in W8.md (line 160): cross-panel refresh is basic, mission-cost burn-down is not persisted, and outward tools/list_changed is missing. The code matches that: tools are refreshed by re-running listTools() in externalMcpService.ts (line 469), usage events are only an in-memory ring buffer in externalMcpService.ts (line 330) and externalMcpService.ts (line 853), and the settings surfaces are mount-fetch only in ExternalMcpSection.tsx (line 174) and ComputerUseSection.tsx (line 99).

Medium: release hygiene is still lighter than a tester-ready desktop app probably wants. lint is still a stub in package.json (line 23), so the green bar today is typecheck/tests/build only.

Medium: API keys are still stored as plaintext project-local JSON. The store explicitly says it should move to Electron safeStorage in apiKeyStore.ts (line 5). For a local-first app this is understandable, but it is still a real security hardening gap.

Low: root guidance is stale enough to cause future drift. AGENTS.md (line 1) still has the first-time placeholder and frames the repo as a Mintlify docs project, which no longer matches the actual desktop app work.

Low, perf: build succeeds, but there is still meaningful renderer weight. The production build emitted very large chunks, especially HistoryPage at about 4.65 MB, Monaco vendor at about 4.29 MB, and the main index chunk at about 1.58 MB minified. Not a blocker, but still worthwhile if you want another post-v1 perf pass.

Verification

Desktop typecheck passed. Desktop tests passed 131/131 files and 1207/1207 tests. MCP typecheck/build passed, and MCP tests passed 88/88. Desktop production build also succeeded. I didn’t make code changes.

Bottom line

I do think you’re basically right: phases 1-5 are genuinely in “single-device v1” territory now. The remaining missing implementation is concentrated in computer-use proof automation, external MCP polish, and ops/security hardening, not in the core lane/orchestrator/runtime foundation.

From the inspiration pass, the best post-v1 UX wins still look like: a stronger review/approval surface, clearer task-tracker-vs-chat separation, and richer persistent audit telemetry for external tools and proof artifacts. That read was mainly informed by Claude agent teams, OpenAI Codex review, Symphony, Paperclip, and Graphite AI reviews.

If you want, I can turn this into a concrete “Phase 5.1 hardening checklist” with owners, priority, and exact fix order.
